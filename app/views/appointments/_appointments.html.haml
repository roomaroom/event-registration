%table.table
  %thead
    %tr
      %th Ім'я
      %th Телефон
      %th Нотатка
      %th Заплановано Оплатити
      %th Оплачено
      %th Title
      %th Зберегти
      %th
      %th
      %th

  %tbody
    - @appointments.each do |appointment|
      %tr
        = simple_form_for(appointment) do |f|
          = f.error_notification
          / %td

          /   .form-inputs
          /     = f.input :notice
          /   .form-actions
          /     = f.button :submit
          %td.active= appointment.user.name
          %td.warning= appointment.user.mobile
          %td.success
            .form-inputs
              = f.input :notice, label: false
          %td.info
            = f.object.payment
          %td.active
            .form-inputs
              = f.input :paid, label: false
          %td.warning= appointment.event.title
          %td.active
            .form-actions
              /= f.button :submit
              = f.submit 'Зберегти', class: 'btn btn-primary save'
   
        /   %td= link_to 'Show', appointment
        /   %td= link_to 'Edit', edit_appointment_path(appointment)
        /   %td= link_to 'Destroy', appointment, :method => :delete, :data => { :confirm => 'Are you sure?' }
